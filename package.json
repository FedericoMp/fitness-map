{
  "name": "fitness-map",
  "version": "1.0.0",
  "description": "Web Applicaiton built with frontend tools and based on OpenStreen Maps with information about physical activities and sports.",
  "type": "module",
  "scripts": {
    "build": "npm run app && node dist",
    "app": "esbuild app=src/app/index.js home=src/styles/home.css info=src/styles/info.css --minify --bundle --external:./node_modules/* --outdir=dist --watch"
  },
  "author": "Federico Perez",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.25.5"
  }
}
